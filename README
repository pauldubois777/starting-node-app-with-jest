# Starting Node App with Jest

This app is a simple Node application with the only dependency being `jest`

To use it:
- Clone this repo
- Run `npm install`
- Run tests `npm test`
- Run the app `node index.js`